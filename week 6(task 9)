Given a string s, return the longest palindromic substring in s.

 

Example 1:

Input: s = "babad"
Output: "bab"
Explanation: "aba" is also a valid answer.
Example 2:

Input: s = "cbbd"
Output: "bb"
 

Constraints:

1 <= s.length <= 1000
s consist of only digits and English letters
Program:
public class Solution {
    public String longestPalindrome(String s) {
        if (s == null || s.length() < 1) return "";
        
        int start = 0;
        int end = 0;
        
        for (int i = 0; i < s.length(); i++) {
            // Case 1: Odd length palindrome centered at index i
            int len1 = expandAroundCenter(s, i, i);
            
            // Case 2: Even length palindrome centered between i and i+1
            int len2 = expandAroundCenter(s, i, i + 1);
            
            // Get the maximum length found from this center position
            int maxLen = Math.max(len1, len2);
            
            // Update the global longest boundary window if a longer one is found
            if (maxLen > (end - start)) {
                start = i - (maxLen - 1) / 2;
                end = i + maxLen / 2;
            }
        }
        
        return s.substring(start, end + 1);
    }
    
    private int expandAroundCenter(String s, int left, int right) {
        // Expand outward as long as pointers are valid and characters match
        while (left >= 0 && right < s.length() && s.charAt(left) == s.charAt(right)) {
            left--;
            right++;
        }
        // Return the length of the valid palindromic segment
        return right - left - 1;
    }
}
Output:
Input
s =
"babad"
Output
"aba"
